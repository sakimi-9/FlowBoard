# 更新记录

## 2025-12-05
- 重写日历页面单元格渲染：改用自定义 `dateFullCellRender`，在竖屏展示进度条、桌面展示任务列表，并确保日期数字只渲染一次。
- 为两种布局补上日历格边框与统一的任务承载框，桌面端默认仅显示 3 条任务、可滚动查看更多且滚动条隐藏。
- 进一步缩小横屏任务框宽高并贴左显示标题，同时去除“暂无任务”提示；竖屏模式压缩进度条与底部留白，便于后续按注释调节尺寸。
